@model ViewTeamRole

<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Description</p>
    </div>
    <div class="card-body">
        <a href="#" class="card-link">Удалить роль</a>
        <a href="#" class="card-link">Изменить роль</a>
    </div>
</div>